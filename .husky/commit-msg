#!/bin/sh

# Get the absolute path to the commit message file
COMMIT_MSG_FILE=$(cd "$(dirname "$1")" && pwd)/$(basename "$1")

# Run commitlint from the fe workspace where packages are installed
cd fe && pnpm exec commitlint --edit "$COMMIT_MSG_FILE"
